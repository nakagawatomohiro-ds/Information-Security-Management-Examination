"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
const navItems=[{href:"/",label:"ホーム",icon:"🏠"},{href:"/stages",label:"ステージ",icon:"📋"},{href:"/review",label:"復習",icon:"🔄"},{href:"/progress",label:"進捗",icon:"📊"}];
export function BottomNav(){const pathname=usePathname();if(pathname.startsWith("/quiz"))return null;return(<nav className="fixed bottom-0 left-0 right-0 bg-white/95 backdrop-blur-sm border-t border-brand-200 safe-bottom z-50"><div className="max-w-lg mx-auto flex justify-around">{navItems.map((item)=>{const isActive=item.href==="/"?pathname==="/":pathname.startsWith(item.href);return(<Link key={item.href} href={item.href} className={`flex flex-col items-center py-2 px-4 min-w-[64px] transition-colors ${isActive?"text-brand-700 font-semibold":"text-slate-400 active:text-slate-600"}`}><span className="text-xl">{item.icon}</span>span><span className="text-xs mt-0.5 font-medium">{item.label}</span>span>{isActive&&<span className="w-1 h-1 rounded-full bg-brand-500 mt-0.5"/>}</Link>Link>);})}</div>div></nav>nav>);}</nav>
